# Figure 4

`figure_4_reference.pdf` is the frozen paper-facing artifact. Run
`python plot_reddit_replication.py` to reproduce the exploratory and matched
three-seed Reddit panels from `input/`. The default output is
`reproduced/figure_4.pdf`, which does not replace the reference PDF.
