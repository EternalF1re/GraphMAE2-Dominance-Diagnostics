# Figure 1

`figure_1_reference.pdf` is the frozen paper-facing artifact. Run
`python plot_validation_trajectory.py` to read the released input under
`input/` and write `reproduced/figure_1.pdf`. The rerun never overwrites the
reference PDF and is intentionally excluded from the frozen manifest.
