# Appendix conflict distribution

`appendix_conflicts_reference.pdf` is the frozen paper-facing artifact. Run
`python plot_pcgrad_conflicts.py` to use the released 0.025-width histogram
bins containing 25,596 optimizer updates pooled over three pretraining seeds.
The default output is `reproduced/appendix_conflicts.pdf`, outside the frozen
manifest.
