# Figure 3

`figure_3_reference.pdf` is the frozen paper-facing artifact. Run
`python plot_protocol_parameters.py` to reproduce the gamma and remasking
response panels from `input/`; the default output is
`reproduced/figure_3.pdf`, outside the frozen manifest.
